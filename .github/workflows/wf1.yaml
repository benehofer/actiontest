name: workflow1

on:
  push

jobs:
  job1:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
    - name: Integration-plan
      shell: pwsh
      run: .\script1.ps1
      env:
          environment: 'Integration's
